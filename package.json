{
  "name": "moviesmod-vercel",
  "version": "1.0.0",
  "description": "MoviesMod Bypasser API for Vercel",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "start": "npm run build && vercel start",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "base-64": "^1.0.0",
    "utf8": "^3.0.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}